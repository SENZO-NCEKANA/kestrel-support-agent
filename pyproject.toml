[tool.pytest.ini_options]
# The package is never pip-installed — CI runs `pip install -r requirements.txt`
# and nothing else. pythonpath puts src/ on sys.path so `from kestrel.chunking
# import ...` resolves in tests without a conftest sys.path hack.
pythonpath = ["src"]
testpaths = ["tests"]
